from typing import List

def storageOptimization(n: int, m: int, h: List[int], v: List[int]) -> int:
    storageX = [0 for i in range(n * 2 + 1)]
    storageY = [0 for j in range(m * 2 + 1)]
    for i in h:
        storageY[]
    return 0
